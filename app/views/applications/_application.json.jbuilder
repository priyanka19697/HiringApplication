json.extract! application, :id, :name, :email, :mobile, :experience, :resume, :status_id, :job_id, :joining_date, :rejection_reason, :created_at, :updated_at
json.url application_url(application, format: :json)
